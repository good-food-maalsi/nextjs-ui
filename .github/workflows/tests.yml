name: Tests CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  unit-tests:
    name: Unit Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 9

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Generate Prisma Client
        # Dummy DATABASE_URL required because prisma.config.ts validates this variable
        # even though it's not used during client generation (only the schema is needed)
        env:
          DATABASE_URL: postgresql://user:password@localhost:5432/dummy?schema=public
        run: pnpm prisma generate

      - name: Run unit tests
        run: pnpm test:backend

      - name: Upload coverage
        uses: codecov/codecov-action@v3
        with:
          files: ./coverage/coverage-final.json
          flags: unittests
        if: always()

  integration-tests:
    name: Integration Tests
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: goodfood_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5433:5432

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 9

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Generate Prisma Client
        # DATABASE_URL required because prisma.config.ts validates this variable
        # even though it's not used during client generation (only the schema is needed)
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5433/goodfood_test?schema=public
        run: pnpm prisma generate

      - name: Run migrations
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5433/goodfood_test?schema=public
        run: pnpm prisma migrate deploy

      - name: Run integration tests
        # Skip test:prepare (Docker setup) as we use GitHub Actions PostgreSQL service
        # Migrations are already run in the previous step
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5433/goodfood_test?schema=public
          JWT_SECRET: test-secret-key
        run: pnpm vitest run --config vitest.integration.config.ts

      - name: Upload integration test results
        uses: actions/upload-artifact@v4
        with:
          name: integration-test-results
          path: coverage/
        if: always()

  lint-and-typecheck:
    name: Lint & Type Check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 9

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Generate Prisma Client
        # Dummy DATABASE_URL required because prisma.config.ts validates this variable
        # even though it's not used during client generation (only the schema is needed)
        env:
          DATABASE_URL: postgresql://user:password@localhost:5432/dummy?schema=public
        run: pnpm prisma generate

      - name: Run ESLint
        run: pnpm lint

      - name: Run TypeScript type check
        run: pnpm typecheck
